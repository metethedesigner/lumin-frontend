import { Box, Typography } from '@mui/material';

export const Hero = () => {
  return (
      <Box className="hero">
         <Typography variant="h2" component="h1" className="hero-title">
        Welcome to LumFlights
      </Typography>
      </Box>
  );
};